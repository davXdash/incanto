async function initEditorialHighlights(){const attimi=document.querySelector('#attimiGrid'),funds=document.querySelector('#fundstueckeGrid');if(!attimi||!funds)return;try{const response=await fetch('editorial-highlights.json?v=20260722-1');if(!response.ok)throw new Error('Redaktionelle Inhalte konnten nicht geladen werden.');const items=await response.json();const card=item=>`<article class="curated-card"><img src="${item.image}" alt=""><div class="curated-copy"><span class="eyebrow">${item.region}</span><h3>${item.title}</h3><p class="curated-place">${item.place}</p><p>${item.description}</p><p class="curated-why"><strong>Warum hier:</strong> ${item.why}</p><button type="button" data-open-route="${item.routeId}">PASSENDE ROUTE ÖFFNEN →</button></div></article>`;attimi.innerHTML=items.filter(item=>item.category==='attimo').map(card).join('');funds.innerHTML=items.filter(item=>item.category==='fund').map(card).join('');document.querySelectorAll('.curated-card [data-open-route]').forEach(button=>button.addEventListener('click',()=>window.openRoute?.(button.dataset.openRoute,{source:'curated',sequence:items.filter(item=>item.category===button.closest('#attimiGrid')?'attimo':'fund').map(item=>item.routeId)})))}catch(error){attimi.innerHTML=`<p>${error.message}</p>`;funds.innerHTML=`<p>${error.message}</p>`}}if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',initEditorialHighlights,{once:true});else initEditorialHighlights();